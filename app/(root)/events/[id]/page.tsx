import { SearchParamProps } from "@/types";
import React from "react";

const EventlyDetails = async ({ params: { id } }: SearchParamProps) => {
  return <div>EventlyDetails</div>;
};

export default EventlyDetails;
